// import { createAsyncThunk, createSlice } from '@reduxjs/toolkit';

export interface AuthState {
  token: string | null;
}

// const initialState: AuthState = {
//   token: null,
// };

// export const signInUser = createAsyncThunk('signinuser', async thunkApi => {
//   // const response = await
// });
